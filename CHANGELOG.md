# تغییرات پروژه

## پروتکل گیت
- قواعد نادیده‌گیری برای فایل‌های محیطی، توکن‌ها، اعتبارنامه‌ها و فایل‌های موقت احراز هویت اضافه شد.
- این تغییر فقط از ورود فایل‌های حساس و موقت به گیت پیشگیری می‌کند؛ داده‌ها، شواهد و امتیازدهی مدل تغییر نکرده‌اند.

## استپ 9A — برچسب‌های مثبت
- ۲۵ رکورد کلاس A ثبت شد: ۲ منگنز، ۱ کرومیت و ۲۲ نقطه نمونه آهن از یک جدول پژوهشی قطرویه؛ caveatهای datum/دقت و وابستگی مکانی ثبت شدند.
- CSV، GeoJSON کلاس A، روش پیشنهادی منفی‌سازی بدون ساخت نقطه، گزارش فارسی و مانیفست SHA256 همه GeoJSONها به‌روز شدند.
- داده‌ها فقط مرجع باقی ماندند؛ مدل، آموزش، امتیازدهی، وزن‌ها و رتبه‌بندی‌ها تغییر نکردند.
